import { createStore } from 'vuex'
import auth from '@/store/register'

export default createStore({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        auth: auth
    }
})
